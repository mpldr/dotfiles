[filter "lfs"]
	process  = git-lfs filter-process
	required = true
	clean    = git-lfs clean -- %f
	smudge   = git-lfs smudge -- %f
[user]
	name             = Moritz Poldrack
	email            = git@moritz.sh
[credential]
	helper           = store
	credentialStore  = secretservice
[pull]
	rebase           = true
[commit]
	gpgsign          = true
[init]
	defaultBranch    = master
	templatedir      = /home/moritz/.git-templates
[merge]
	ff               = false
	directoryRenames = true
	tool             = vimdiff
	guitool          = smerge
[diff]
	renames          = true
[format]
	signoff          = true
[include]
	path             = .gitconfig.local

[includeIf "gitdir:Projects/Arbeit/"]
	path             = .gitconfig.work
[http]
	sslVerify = false
[sendemail]
	chainreplyto       = false
	from               = Moritz Poldrack <moritz@poldrack.dev>
	assume8bitEncoding = utf-8
	annotate           = true
	confirm            = auto
	sendmailcmd        = msmtpq
[rerere]
	enabled = 1
[core]
	hooksPath = /home/moritz/.githooks
	quotePath = false
	commitGraph = true
[advice]
	detachedHead = false
	addIgnoredFile = false
[filter "gofmt"]
	smudge = cat
	clean = gofumpt -s
[filter "vuefmt"]
	smudge = cat
	clean = yarn run -s prettier --parser vue --use-tabs --print-width 100 --end-of-line lf
[filter "jsfmt"]
	smudge = cat
	clean = yarn run -s prettier --parser babel --use-tabs --print-width 100 --end-of-line lf
[filter "cssfmt"]
	smudge = cat
	clean = yarn run -s prettier --parser css --use-tabs --print-width 100 --end-of-line lf
[receive]
	advertisePushOptions = true
	procReceiveRefs = refs/for
[gc]
	writeCommitGraph = true
[tag]
	sort=-v:refname
